{
  "name": "fchat",
  "version": "3.0.0",
  "author": "The F-List Team",
  "description": "F-Chat Themes",
  "license": "MIT",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.6.3",
    "bootstrap": "^4.2.1"
  },
  "devDependencies": {
    "sass-embedded": "^1.89.2"
  },
  "scripts": {
    "build": "sass --load-path=. --style=compressed --source-map --importer=./importer.js --update themes/chat:css themes/site:css"
  }
}
